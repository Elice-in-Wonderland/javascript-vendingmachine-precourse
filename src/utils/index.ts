import {
  deepFreeze,
  removeFirstLetter,
  formDataToObject,
  every,
} from './common';
import { $, $$, createElement, delegate, emit } from './dom';
import { isValidProduct } from './validate';

export {
  deepFreeze,
  removeFirstLetter,
  formDataToObject,
  every,
  $,
  $$,
  createElement,
  delegate,
  emit,
  isValidProduct,
};
